# Core web framework
flask>=2.3.0
gunicorn>=21.0.0

# For TF-IDF matching (using pure Python implementation, no sklearn needed)
numpy>=1.24.0

# Optional: Neural matching (uncomment for semantic matching support)
# Note: This significantly increases deployment size and startup time
# sentence-transformers>=2.2.0
# torch>=2.0.0
